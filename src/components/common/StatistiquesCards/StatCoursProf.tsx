// src/components/teacher/StatCardTeacher.tsx
import React from 'react';
import StatCard, { StatCardProps } from './StatCard';

const StatCardTeacher = (props: StatCardProps) => {
  return <StatCard {...props} />;
};

export default StatCardTeacher;
