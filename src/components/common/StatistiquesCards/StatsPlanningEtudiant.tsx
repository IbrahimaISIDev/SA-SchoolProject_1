// src/components/student/StatCardEtudiant.tsx
import React from 'react';
import StatCard, { StatCardProps } from './StatCard';

const StatCardEtudiant = (props: StatCardProps) => {
  return <StatCard {...props} />;
};

export default StatCardEtudiant;
